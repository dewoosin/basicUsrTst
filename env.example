# 환경변수 설정 예시 파일
# 실제 운영환경에서는 이 파일을 .env로 복사하고 실제 값으로 변경하세요

# JWT 설정
JWT_SECRET=your-base64-encoded-secret-key-here
JWT_ACCESS_TOKEN_EXPIRATION=900000
JWT_REFRESH_TOKEN_EXPIRATION=604800000

# 데이터베이스 설정
DB_URL=jdbc:mysql://localhost:3306/nsusTestDb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
DB_USERNAME=your-db-username
DB_PASSWORD=your-db-password

# Redis 설정
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password

# JWT 시크릿 키 생성 방법:
# 1. OpenSSL 사용: openssl rand -base64 32
# 2. Java 코드 사용: java.util.Base64.getEncoder().encodeToString(new byte[32])
# 3. 온라인 도구: https://generate-secret.vercel.app/32
